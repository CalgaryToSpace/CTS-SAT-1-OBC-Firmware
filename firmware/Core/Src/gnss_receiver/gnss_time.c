#include <stdint.h>
#include <string.h>

/// @brief Sets the OBC's time, based on the GNS receiver's current time.
/// @return 0 on success, >0 on failure.
uint8_t GNSS_set_obc_time_based_on_gnss_time() {

    return 0;
}
