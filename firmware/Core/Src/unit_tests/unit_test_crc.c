#include <stdint.h>
int main(void) {
/*
    // Test the CRC function
    uint32_t short_input[] = {0x01, 0x02};
    uint32_t long_input[] = {0x01, 0x02, 0x03, 0x04, 0x05, 0x06, 0x07, 0x08, 0x09, 0x0A, 0x0B, 0x0C, 0x0D, 0x0E, 0x0F, 0x10};
    uint32_t null_input[] = {0x00, 0x00, 0x00, 0x00};
    uint32_t checksum;

    GEN_crc32_checksum(short_input, sizeof(short_input), &checksum);
    printf("Short Input Checksum: %08X\n", checksum);

    GEN_crc32_checksum(long_input, sizeof(long_input), &checksum);
    printf("Long Input Checksum: %08X\n", checksum);

    GEN_crc32_checksum(null_input, sizeof(null_input), &checksum);
    printf("Null Input Checksum: %08X\n", checksum);

    // Example
    uint8_t random_input[] = {0x12, 0x34, 0x56, 0x78, 0x9A, 0xBC, 0xDE, 0xF0};
    GEN_crc32_checksum(random_input, sizeof(random_input), &checksum);
    printf("Random Input Checksum: %08X\n", checksum);
    
    while (1) {
        // Main loop
    }

*/
}
